name: Quirk Semantic Check

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  semantic-governance:
    uses: Quirk-Systems/.github/.github/workflows/quirk-semantic-governance.yml@main
