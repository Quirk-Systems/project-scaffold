# Canonical ontology

Place governed entity definitions in domain folders under this directory
(`types`, `moves`, `capabilities`, `personas`, `characters`, `realms`,
`systems`, and `governance`). Initial bootstrap records live in `../seeds`;
the loader treats both trees as Git-authoritative inputs.
